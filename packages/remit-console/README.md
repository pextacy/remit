# remit-console

Next.js operator console: review queue (G3), receipt ledger, Remit screen, kill switch.

Not scaffolded in P0 on purpose. It lands in P9 (PLAN.md 4.4), and under cut lines C3
and C7 it degrades to a CLI approval prompt plus a static page generated from
`receipts/`. The G3 gate itself is never cut — only its UI.
